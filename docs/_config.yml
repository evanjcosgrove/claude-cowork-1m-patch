theme: minima
title: claude-cowork-1m-patch
description: How a server-side feature flag rollback silently downgraded Claude Desktop's Cowork mode from 1M to 200K context, and what it took to put it back — including the four-layer integrity wall and two same-length JS swaps.
markdown: kramdown
exclude:
  - integrity-layers.md
  - root-cause-analysis.md
